## Study Project - interactive quiz on the topic "HTML"

- User chooses different answers and gets a bonus at the end
- Implemented work with DOM (events)
- There is work with inputs, checkboxes, you can go back
